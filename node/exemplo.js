// 01. DECLARACAO — variavel de controle
let i = 1;

// 02. CONDICAO verificada antes de cada iteracao
while (i <= 5) {

    // 03. PROCESSAMENTO — o que fazer em cada repeticao
    console.log("Numero: " + i);

    // 04. ATUALIZACAO — modifica a variavel de controle
    i++;
}